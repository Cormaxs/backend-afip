import { Router } from "express";

export const afip_Router = Router();
/*
//generar clave  privada y solicitud del certificado, le brindo la solicitud del certificado, el usuario crea el certificado en afip
afip_Router.post("/key-csr",);

//pone el cdigo del certificado para que lo cree en la misma carpeta de lo demas
afip_Router.post("/create-crt",);

//consigue el token y sign
afip_Router.post("/get-ta",);

//manda los datos de la factura para obtener el CAE, si esta aprobado pasoa generar la factura
afip_Router.post("/get-cae",);
*/
